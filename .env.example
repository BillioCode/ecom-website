# Server Port for the Node.js Express backend.
# You can use any available port, 5001 is common.
PORT=5001

# MongoDB Connection String
# Get this from your MongoDB Atlas dashboard (replace `<username>`, `<password>`, `myFirstDatabase`, `myClusterName`).
# Or, if using a local MongoDB, it might be: mongodb://localhost:27017/your_database_name
MONGO_URI=your_mongodb_atlas_connection_string_here

# Redis URL for caching and session management (e.g., Upstash Redis)
# Get this from your Redis provider's dashboard (e.g., Upstash).
UPSTASH_REDIS_URL=your_upstash_redis_url_here

# JWT Secret Keys
# These should be very long, complex, and randomly generated strings for security.
# DO NOT use "access_token" or "refresh_token_secret" in your actual .env file.
# You can generate them online or programmatically.
ACCESS_TOKEN_SECRET=your_jwt_access_token_secret_very_long_and_random
REFRESH_TOKEN_SECRET=your_jwt_refresh_token_secret_another_long_and_random_string

## Cloudinary API Credentials (for image storage)
# Get these from your Cloudinary dashboard.
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Stripe Secret Key (for processing payments)
# Get this from your Stripe Dashboard -> Developers -> API keys.
# Ensure it's your **Secret key**, not the Publishable key.
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Frontend Client URL (for CORS and redirects)
# This should be the URL where your React frontend is running.
# In development, it's typically http://localhost:5173 (for Vite) or http://localhost:3000 (for Create React App).
# In production, it would be your deployed frontend URL.
FRONTEND_URL=http://localhost:5173

# Node.js Environment
# 'development' for local development, 'production' for deployed application.
NODE_ENV=development